<div class="row">
    <div class="col-lg-2"></div>
    <div class="col-lg-8">
        <h1>Chi tiết sản phẩm</h1>
        <form role="form" action = "/san-pham/{{product._id}}/edit"
        method="post" enctype ="multipart/form-data">
            <div class="form-group">
                <label>Name</label>
                <input class="form-control" value="{{product.name}}" type="text" name="name">
            </div>
            <div class="form-group">
                <label>Price</label>
                <input class="form-control" value="{{product.price}}" type="number" name="price">
            </div>
            <div class="form-group">
                <label>Quantity</label>
                <input class="form-control" value="{{product.quantity}}" type="number" name="quantity">
            </div>
            <div class="form-group">
                <label>Image</label>
                <input type="file" name="image" id = "image-file" onchange ="onChangeFile()">
                <img src="{{product.image}}" id="image-view" style="object-fit: cover;" width="200" height="200"/>
            </div>
            <div class="form-group">
                <label>Description</label>
                <textarea class="form-control" rows="3"
                    name="description">{{product.description}}</textarea>
            </div>
            
            
            <div class="form-group">
                <label>Category</label>
                <select class="form-control" name="category_id">
                    {{#each categories}}
                    <option value="{{this._id}}">{{this.name}}</option>
                    {{/each}}
                </select>
            </div>
            <div class="form-group">
                <label>Released</label>
                <input class="form-control" type="date" value="{{product.released}}" name="released">
            </div>

            <button type="submit" class="btn btn-primary">Submit</button>
            <button type="reset" class="btn btn-success">Reset</button>
        </form>
    </div>
    <div class="col-lg-2"></div>
</div>
<script>
    const onChangeFile = () => {
        const file = document.getElementById('image-file').files[0];
        const reader = new FileReader();
        reader.onload = e => {
            document.getElementById('image-view').src = e.target.result;
            document.getElementById('image-view').style.display = 'block';
        }
        reader.readAsDataURL(file);
    }
</script>